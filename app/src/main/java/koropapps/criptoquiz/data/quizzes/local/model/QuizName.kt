package koropapps.criptoquiz.data.quizzes.local.model

import koropapps.criptoquiz.R

enum class QuizName(val resId: Int) {
    BITCOIN_BEGINNER(R.string.bitcoin_beginner_name),
    BITCOIN_INTERMEDIATE(R.string.bitcoin_Intermediate_name),
    BITCOIN_HARDCORE(R.string.bitcoin_hardcore_name)
}